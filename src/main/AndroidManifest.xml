<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jixl.e_ticket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eticket"
        android:name="androidx.multidex.MultiDexApplication" >
        <activity
            android:name=".NawActivity"
            android:label="@string/title_activity_naw"
            android:theme="@style/Theme.Eticket.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.SignUp" />
        <activity android:name=".auth.Login" />
        <activity android:name=".auth.MainPage" />
    </application>

</manifest>